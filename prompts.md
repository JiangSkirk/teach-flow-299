# 提示词编排：原始表格 → 摘要 + 邮件草稿

> 用法：按顺序复制到 ChatGPT / Claude（或同类对话工具）。  
> 每步标明 **输入 / 输出**；下一步只用上一步的输出 + 必要原文。  
> **不要**把密钥、完整身份证、未授权客户隐私贴进模型。

---

## 总流程（编排顺序）

```text
[原始表格文本] 
    → Prompt A 结构理解与字段清单
    → Prompt B 清洗建议与问题清单
    → Prompt C 汇总指标（仅基于给定数据）
    → Prompt D 中文邮件草稿
    → （人工）核对数字与收件人后发送
```

---

## Prompt A — 理解表格结构

**输入**：本周原始表格文本（CSV / 粘贴的行列）。  
**输出**：字段列表、主键猜测、明显质量问题初筛（不要编造行）。

```text
你是办公数据分析助理。下面是我粘贴的「本周业务表格」纯文本。
任务：
1) 列出所有列名（若无表头，根据内容猜测并标注【猜测】）。
2) 推断哪一列适合做主键（如单号），不确定则写【待核实】。
3) 统计：总行数、空值较多的列、疑似重复行的线索（不要删除数据）。
4) 用中文简短说明：这份表大概在描述什么业务。
硬性规则：只根据我粘贴的内容；禁止编造单元格数值；不知道就写【待核实】。

【表格文本开始】
（在此粘贴）
【表格文本结束】
```

---

## Prompt B — 清洗建议（人确认后再改表）

**输入**：Prompt A 的输出 + 同一份表格文本（如被截断请重新粘贴）。  
**输出**：可执行的清洗步骤清单 +「必须人工确认」项。

```text
基于上一步的结构分析，以及下面同一份表格文本，请给出「清洗清单」：
1) 建议删除/忽略的行类型（测试、全空、重复表头），每条说明理由。
2) 需要统一的格式（日期、金额、人名）。
3) 空单元格处理建议：哪些可填默认值、哪些必须【待补】。
4) 输出格式：编号列表；每项标注「可自动」或「需人工」。
不要直接输出「已清洗的假数据表」冒充事实；若要给示例，只用【示例】标注的假行。

【表格文本开始】
（在此粘贴，可与 A 相同）
【表格文本结束】
```

---

## Prompt C — 汇总（数字可追溯）

**输入**：你已人工清洗后的表格文本（推荐）+ 汇总口径说明。  
**输出**：要点摘要 + 指标表；每项指标注明计算方式。

```text
你是严谨的周报汇总助理。只使用我提供的「清洗后表格」计算，禁止使用外部知识补数。
汇总口径：
- 周期：本周（若表中有日期列，仅统计该范围内；无日期则按全部行并注明）。
- 需要输出：
  1) 核心总量（行数/金额/完成数——以表中实际存在的列为准）。
  2) 分类 Top 5（按负责人或类别列，若无则跳过并说明）。
  3) 风险：逾期、空关键字段、异常大额（定义：金额列中均值 5 倍以上且标注【待核实】）。
  4) 与「上周数字」对比：若我未提供上周数据，写「未提供上周，跳过环比」。
输出结构：
- 「一页纸要点」3～7 条（中文）。
- 「指标明细」用列表，每项含：指标名、数值、计算说明（如 sum(金额)）。
- 末尾「数据局限」：粘贴是否可能截断、缺失字段等。
若某指标算不出，写【数据不足】，不要编造。

【清洗后表格开始】
（在此粘贴清洗后文本）
【清洗后表格结束】

【可选：上周摘要】
（没有就写：无）
```

---

## Prompt D — 邮件草稿

**输入**：Prompt C 的完整输出 + 收件场景参数。  
**输出**：可复制的主题行 + 正文；语气专业简洁。

```text
根据下面的「汇总结果」，写一封中文工作邮件草稿。
场景参数：
- 发件角色：（例：运营助理）
- 收件人称呼：（例：各位同事 / 张经理）
- 目的：通报本周表格汇总，并标明需跟进事项
- 语气：正式、简洁、无夸张
要求：
1) 先给「邮件主题」一行（含周次或日期，勿用夸张词）。
2) 正文：开场一句 → 要点条目 → 风险/待办 → 结尾（如有附件说明「见附件」）。
3) 所有数字必须来自汇总结果；汇总里是【待核实】/【数据不足】的，正文里原样保留，不要改成肯定语气。
4) 不要添加我未提供的业绩承诺、假数据或客套长篇。
5) 最后加一行：「—— 以下由流程教学草稿生成，发送前请人工核对 ——」

【汇总结果开始】
（粘贴 Prompt C 的输出）
【汇总结果结束】
```

---

## Prompt E（可选）— 发送前核对清单

**输入**：邮件草稿 + 你关心的核对点。  
**输出**：是/否核对表；列出必须改的句子。

```text
请作为发送前审稿人，对照「核对点」检查下面邮件草稿：
核对点：
- 是否出现未替换占位符（XX、TODO、【示例】）？
- 是否有与汇总不一致的数字？（若无法判断，列出所有数字清单供人工核对）
- 是否含明显敏感字段（完整手机、身份证）？
- 语气是否像无人值守夸大宣传？
输出：通过项 / 不通过项 / 建议修改的原文摘录。
不要替我重写全文，除非我不通过项 ≥ 3。

【邮件草稿】
（粘贴）
```

---

## 编排备注（给设计者）

| 步骤 | 人 | 模型 | 注意 |
|------|----|------|------|
| A–B | 确认结构与清洗 | 建议与清单 | 清洗落地在表格软件里做 |
| C | 提供清洗后真数据 | 算摘要 | 截断时拆批再合并 |
| D–E | 定稿与发送 | 草稿与审稿辅助 | **发送键在人** |

将本文件与 `steps.md`、`exceptions.md` 一起，即构成「可教学的流程设计包」，而非运行中的 Agent。

---

*复制即用 · 发送前必须人工确认*
