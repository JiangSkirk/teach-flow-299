# 步骤清单：每周汇总表格发邮件

> 人可照做；未来自动化也可按同一顺序编排。每步写清**输入 → 动作 → 输出**。  
> 原则：**先清洗再汇总，先草稿再发送，发送后必须归档。**

---

## 0. 准备（每周固定时间）

1. **确认周期**：本周数据截止日（如周日 23:59）与发送日（如周一上午）。  
2. **确认收件人**：主送 / 抄送名单是否有变更。  
3. **确认模板**：邮件主题格式、正文结构、是否需附件。  
   - **输出**：本周「发送日 + 收件人 + 模板版本」三件套确认无误。

---

## 1. 导出 / 打开表格

4. 打开源表（Excel / 飞书 / 钉钉 / Google Sheets / WPS 等）。  
5. 确认打开的是**正确工作表**（名称、月份、项目与本周一致）。  
6. 导出或复制本周相关区域为：  
   - 可编辑副本（`.xlsx` / CSV），或  
   - 纯文本表格（制表符分隔），供后续粘贴到大模型。  
   - **输入**：源系统权限、表名、日期筛选条件。  
   - **输出**：本周原始数据副本（文件或文本）。

---

## 2. 清洗

7. 删除明显无关行（测试行、合计行重复、空白抬头）。  
8. 统一字段：日期格式、金额小数位、部门/负责人名称写法。  
9. 处理空单元格：标注「待补」或按业务规则填默认值（规则写在 `exceptions.md`）。  
10. 去重：同一主键（如订单号/工单号）重复时保留最新或按规则合并。  
    - **输入**：原始数据副本。  
    - **输出**：清洗后表格 + 简短清洗备注（改了什么）。

---

## 3. 汇总

11. 按约定维度汇总，例如：  
    - 本周总量（行数 / 金额 / 完成数）  
    - 环比或与上周对比（若有上周归档）  
    - Top N（负责人、品类、异常类型）  
    - 风险项：逾期、缺字段、异常金额  
12. 生成「一页纸」摘要要点（3–7 条），数字可核对回原表。  
    - **输入**：清洗后表格。  
    - **输出**：汇总表或要点列表（可另存 `summary-本周.md` / 截图）。

---

## 4. 起草邮件

13. 用 `prompts.md` 中的提示词：粘贴清洗后表格文本 → 得到摘要 + 邮件草稿。  
14. **人工校对**：数字、人名、截止日期、语气、保密信息。  
15. 填入主题、主送、抄送；需要时附上汇总表或 PDF。  
    - **输入**：汇总要点 + 收件人名单 + 模板。  
    - **输出**：待发送邮件草稿（未点发送）。

---

## 5. 发送（人工确认后）

16. 最终核对：收件人、附件、是否回复上一封线程。  
17. 在约定发送窗口内点击发送（或由授权同事发送）。  
18. 记录发送时间与操作人。  
    - **输入**：已审阅草稿。  
    - **输出**：已发送邮件 + 发送日志一行。

---

## 6. 归档

19. 将「清洗后表 / 汇总 / 最终邮件正文 / 发送时间」存入固定文件夹或知识库，命名示例：`YYYY-WW-周报汇总/`。  
20. 更新台账：本周是否按时、有无异常、下周一句改进。  
21. 若有未闭合异常（空数据、权限失败等），写入下周待办。  
    - **输入**：本周全部产物。  
    - **输出**：可检索的归档包 + 台账更新。

---

## 检查清单（发送前 30 秒）

- [ ] 表是对本周、对正确 sheet  
- [ ] 关键数字与表一致  
- [ ] 邮件无未替换占位符（如「XX部门」）  
- [ ] 收件人正确；附件非旧文件  
- [ ] 敏感列已脱敏或已获准外发  
- [ ] 发送后归档路径已定

---

*对应场景教学样本 · 非自动运行 Agent*
